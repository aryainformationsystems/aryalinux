function cleanup() {
	cd /sources
	rm -rf $1
	rm -rf gcc-build
	rm -rf glibc-build
	rm -rf binutils-build
}

function is_logged() {
	grep -Fxq "$1" /sources/build-log 2>/dev/null
}

function log() {
	if ! is_logged "$1"; then
		echo "$1" >> /sources/build-log
	fi
}
